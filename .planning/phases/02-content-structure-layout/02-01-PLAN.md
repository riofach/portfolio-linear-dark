---
phase: 02-content-structure-layout
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/sections/Hero.tsx
autonomous: true
must_haves:
  truths:
    - "Hero section renders with 3D grid classes"
    - "Hero contains 'View My Work' and 'Contact Me' buttons"
  artifacts:
    - path: src/components/sections/Hero.tsx
      contains: "perspective"
  key_links: []
---

<objective>
Implement the Hero section with a 3D perspective grid background and centered typography.

Purpose: specific design requirement ("floor" effect) for the "linear-dark" aesthetic.
Output: `src/components/sections/Hero.tsx` ready for import.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/app/globals.css

# Tailwind v4 3D transforms
# Use utility classes: perspective-[value], rotate-x-[value], transform-3d
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Hero Component with 3D Grid</name>
  <files>src/components/sections/Hero.tsx</files>
  <action>
    Create `src/components/sections/Hero.tsx`.
    
    Implementation details:
    1. Structure: Full-screen section (`min-h-screen`, `flex-center`).
    2. Background:
       - Container: `perspective-[1000px] overflow-hidden`
       - Grid Plane: Absolute, `inset-0`, `origin-top`, `rotate-x-[60deg]`, `bg-[linear-gradient(...)]` (or use CSS grid pattern).
       - Fog: Gradient mask to fade out the top (near horizon).
    3. Content:
       - H1: "Building the future..." (or similar placeholder), `text-balance`, `text-center`.
       - Text Glow: Use `drop-shadow` or `text-shadow` (via arbitrary values if needed).
       - Buttons: Primary (White bg, black text) and Secondary (Glass).
  </action>
  <verify>
    grep "perspective" src/components/sections/Hero.tsx && grep "rotate-x" src/components/sections/Hero.tsx
  </verify>
  <done>
    File exists and contains 3D transform utility classes.
  </done>
</task>

</tasks>

<verification>
# Automated checks
grep "perspective" src/components/sections/Hero.tsx
</verification>

<success_criteria>
- [ ] Hero component exists
- [ ] 3D grid background implemented via CSS transforms
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-structure-layout/02-01-SUMMARY.md`
</output>
