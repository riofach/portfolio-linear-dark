---
phase: 02-content-structure-layout
plan: 05
type: execute
wave: 2
depends_on: [01, 02, 03, 04]
files_modified:
  - src/app/page.tsx
  - src/app/layout.tsx
autonomous: false
must_haves:
  truths:
    - "Homepage renders all sections in order"
    - "User can scroll from Hero to Footer"
  artifacts:
    - path: src/app/page.tsx
      contains: "<Hero />"
  key_links:
    - from: src/app/page.tsx
      to: src/components/sections/Hero.tsx
      via: import
---

<objective>
Compose the main homepage by integrating all created sections and verifying the responsive layout.

Purpose: Assemble the separate components into the final page structure.
Output: Updated `page.tsx` and verification report.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@src/components/sections/Hero.tsx
@src/components/sections/About.tsx
@src/components/sections/Skills.tsx
@src/components/sections/Projects.tsx
@src/components/sections/Experience.tsx
@src/components/sections/Contact.tsx
@src/components/layout/Footer.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Footer to Layout</name>
  <files>src/app/layout.tsx</files>
  <action>
    Import `Footer` from `src/components/layout/Footer` and add it to `src/app/layout.tsx` (inside `body`, after `{children}`).
    Ensure it sits at the bottom of the page structure.
  </action>
  <verify>
    grep "<Footer />" src/app/layout.tsx
  </verify>
  <done>
    Footer added to global layout.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compose Homepage</name>
  <files>src/app/page.tsx</files>
  <action>
    Update `src/app/page.tsx`.
    1. Import all sections: Hero, About, Skills, Projects, Experience, Contact.
    2. Render them in order:
       - Hero (id="home")
       - About (id="about")
       - Skills (id="skills")
       - Projects (id="work" or "projects")
       - Experience (id="experience")
       - Contact (id="contact")
    3. Ensure IDs match the Navigation Links from Phase 1.
  </action>
  <verify>
    grep "<Hero />" src/app/page.tsx && grep 'id="about"' src/app/page.tsx
  </verify>
  <done>
    Page composes all sections with correct IDs.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full Homepage Layout (Hero, About, Skills, Projects, Experience, Contact, Footer)</what-built>
  <how-to-verify>
    1. Run `npm run dev`.
    2. Open `http://localhost:3000`.
    3. Verify Hero 3D grid effect.
    4. Verify scroll spy works (Nav highlights as you scroll sections).
    5. Check responsive behavior on mobile (About stack, Grid columns).
  </how-to-verify>
  <resume-signal>Type "approved" if layout looks correct, or describe layout issues.</resume-signal>
</task>

</tasks>

<verification>
# Automated checks
grep "<Hero />" src/app/page.tsx
</verification>

<success_criteria>
- [ ] Homepage assembles all sections
- [ ] Responsive layout verified by human
</success_criteria>

<output>
After completion, create `.planning/phases/02-content-structure-layout/02-05-SUMMARY.md`
</output>
