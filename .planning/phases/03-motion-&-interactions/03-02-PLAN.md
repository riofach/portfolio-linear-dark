---
phase: 03-motion-&-interactions
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/sections/hero-section.tsx
  - src/components/sections/about-section.tsx
  - src/components/sections/skills-section.tsx
  - src/components/sections/projects-section.tsx
  - src/components/sections/experience-section.tsx
  - src/components/sections/contact-section.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Hero text enters with staggered animation on load"
    - "Sections fade up gently as user scrolls"
    - "Animations play only once (no replay on scroll up)"
  artifacts:
    - path: "src/components/sections/hero-section.tsx"
      provides: "Hero with entrance animation"
      contains: "staggerChildren"
  key_links:
    - from: "src/components/sections/about-section.tsx"
      to: "src/components/ui/fade-in.tsx"
      via: "import FadeIn"
---

<objective>
Apply entrance and scroll reveal animations to all main sections using the motion infrastructure.

Purpose: Bring the page to life with the "linear-dark" fade-up aesthetic.
Output: Animated Hero, About, Skills, Projects, Experience, and Contact sections.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-motion-&-interactions/03-01-SUMMARY.md
@src/lib/motion.ts
@src/components/ui/fade-in.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Animate Hero Entrance</name>
  <files>src/components/sections/hero-section.tsx</files>
  <action>
    Modify `HeroSection`:
    - Convert to `motion.div` (or wrap content).
    - Apply `initial="hidden" animate="show"` to container.
    - Variants: `STAGGER_CHILDREN` from `motion.ts`.
    - Apply `FADE_UP_VARIANTS` to children elements (Headline, Subtext, Buttons).
    - Ensure background grid remains static or fades in instantly (per decisions).
    - Keep layout integrity (no layout shifts).
  </action>
  <verify>
    - Check for `motion` imports.
    - Check for `staggerChildren`.
  </verify>
  <done>Hero text staggers in on load.</done>
</task>

<task type="auto">
  <name>Task 2: Apply Scroll Reveals (Group A)</name>
  <files>src/components/sections/about-section.tsx, src/components/sections/skills-section.tsx, src/components/sections/projects-section.tsx</files>
  <action>
    Refactor sections to use `FadeIn`:
    1. `AboutSection`: Wrap main content block in `<FadeIn>`.
    2. `SkillsSection`: Wrap title in `<FadeIn>`. Wrap grid in `<FadeIn delay={0.2}>`.
    3. `ProjectsSection`: Wrap title in `<FadeIn>`. Wrap project grid in `<motion.div>` with `staggerChildren` (using `whileInView`).
  </action>
  <verify>
    - Imports `FadeIn`.
    - Wraps content correctly.
  </verify>
  <done>About, Skills, Projects sections animate on scroll.</done>
</task>

<task type="auto">
  <name>Task 3: Apply Scroll Reveals (Group B)</name>
  <files>src/components/sections/experience-section.tsx, src/components/sections/contact-section.tsx</files>
  <action>
    Refactor sections to use `FadeIn`:
    1. `ExperienceSection`: Wrap title. Use `staggerChildren` for the timeline container.
    2. `ContactSection`: Wrap the entire container in `<FadeIn>`.
  </action>
  <verify>
    - Imports `FadeIn` or uses motion directly.
  </verify>
  <done>Experience and Contact sections animate on scroll.</done>
</task>

</tasks>

<verification>
- All sections import `motion` or `FadeIn`.
- Hero uses `animate="show"` (immediate).
- Other sections use `whileInView` or `FadeIn` (scroll trigger).
</verification>

<success_criteria>
- Hero animations trigger on load.
- Section animations trigger on scroll.
- No layout shifts during animation.
</success_criteria>

<output>
After completion, create `.planning/phases/03-motion-&-interactions/03-02-SUMMARY.md`
</output>
