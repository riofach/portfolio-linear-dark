---
phase: 03-motion-&-interactions
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/ui/project-card.tsx
  - src/components/ui/skill-card.tsx
  - src/components/ui/experience-item.tsx
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Project cards glow/scale slightly on hover"
    - "Skill cards highlight on hover"
    - "Experience items fade in sequentially"
  artifacts:
    - path: "src/components/ui/project-card.tsx"
      provides: "Interactive project card"
      contains: "whileHover"
  key_links:
    - from: "src/components/ui/skill-card.tsx"
      to: "src/lib/motion.ts"
      via: "transition constants"
---

<objective>
Implement micro-interactions for interactive elements (cards, timeline items).

Purpose: Add tactile polish and "alive" feel to the interface.
Output: Interactive Card components.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-motion-&-interactions/03-01-SUMMARY.md
@src/lib/motion.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Project Card Micro-interactions</name>
  <files>src/components/ui/project-card.tsx</files>
  <action>
    Modify `ProjectCard`:
    - Convert to `motion.div` (or wrap).
    - Add `whileHover` state:
      - Scale up slightly (1.02).
      - Border color change or overlay glow opacity increase.
    - Ensure performance (use `layout` prop carefully, prefer transform/opacity).
    - If part of a staggered list, add `variants={FADE_UP_VARIANTS}` to the root.
  </action>
  <verify>
    - `whileHover` prop exists.
    - Scale/Opacity properties used.
  </verify>
  <done>Project cards respond to hover.</done>
</task>

<task type="auto">
  <name>Task 2: Skill Card Micro-interactions</name>
  <files>src/components/ui/skill-card.tsx</files>
  <action>
    Modify `SkillCard`:
    - Convert to `motion.div`.
    - Add `whileHover`:
      - Background lighten or border glow.
    - Add `variants={FADE_UP_VARIANTS}` for staggered entry usage in parent grid.
  </action>
  <verify>
    - `whileHover` prop exists.
  </verify>
  <done>Skill cards respond to hover.</done>
</task>

<task type="auto">
  <name>Task 3: Experience Item Animation</name>
  <files>src/components/ui/experience-item.tsx</files>
  <action>
    Modify `ExperienceItem`:
    - Convert to `motion.div` or `motion.li`.
    - Add `variants={FADE_UP_VARIANTS}` to enable sequential fade-in controlled by parent `ExperienceSection`.
    - Add subtle hover state (optional, maybe just text highlight).
  </action>
  <verify>
    - Imports motion.
    - Exports component capable of being staggered.
  </verify>
  <done>Experience items ready for staggered entry.</done>
</task>

</tasks>

<verification>
- Components use `motion` elements.
- Hover states defined.
- Variants exposed for parent coordination.
</verification>

<success_criteria>
- Cards feel interactive.
- Lists can animate in sequence.
</success_criteria>

<output>
After completion, create `.planning/phases/03-motion-&-interactions/03-03-SUMMARY.md`
</output>
