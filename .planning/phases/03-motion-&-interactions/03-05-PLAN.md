---
phase: 03-motion-&-interactions
plan: 05
type: execute
wave: 4
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - src/app/layout.tsx
  - public/robots.txt
  - src/app/manifest.ts
autonomous: true
user_setup: []

must_haves:
  truths:
    - "Lighthouse Performance score is optimized"
    - "Meta tags (Title, Description) are present"
    - "Robots.txt exists"
  artifacts:
    - path: "src/app/layout.tsx"
      provides: "Metadata export"
      contains: "metadata: Metadata"
    - path: "public/robots.txt"
      provides: "Crawler instructions"
  key_links:
    - from: "src/app/layout.tsx"
      to: "metadata API"
      via: "export metadata"
---

<objective>
Finalize the project with SEO metadata, polish, and performance checks.

Purpose: Ensure the portfolio is production-ready and discoverable.
Output: SEO-optimized application with high performance.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Metadata</name>
  <files>src/app/layout.tsx</files>
  <action>
    Update `metadata` object in `layout.tsx`:
    - Title: "Name | Role" (e.g., "Rio Raditya | Software Engineer").
    - Description: "Portfolio showcasing..."
    - Open Graph: title, description, type, locale.
    - Twitter: card, title, description.
    - Theme Color.
  </action>
  <verify>
    - Check metadata export in layout.
  </verify>
  <done>SEO tags present.</done>
</task>

<task type="auto">
  <name>Task 2: Create Static Assets</name>
  <files>public/robots.txt, src/app/sitemap.ts</files>
  <action>
    1. Create `public/robots.txt`:
       - User-agent: *
       - Allow: /
    2. Create `src/app/sitemap.ts` (Next.js dynamic sitemap):
       - Return base URL route.
  </action>
  <verify>
    - Files exist.
  </verify>
  <done>Robots and Sitemap created.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Final Polish Check</name>
  <what-built>Full Phase 3 Implementation</what-built>
  <how-to-verify>
    1. Run `npm run build` -> `npm start`.
    2. Open in browser.
    3. Verify Hero entrance.
    4. Scroll down - verify section reveals.
    5. Hover cards - verify glow.
    6. Fill form - verify validation.
    7. Run Lighthouse (optional).
  </how-to-verify>
  <resume-signal>Type "approved" if all animations look good.</resume-signal>
</task>

</tasks>

<verification>
- Metadata populated.
- Build succeeds.
</verification>

<success_criteria>
- Project is polished and ready for deployment.
</success_criteria>

<output>
After completion, create `.planning/phases/03-motion-&-interactions/03-05-SUMMARY.md`
</output>
