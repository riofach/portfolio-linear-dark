---
phase: 03-motion-interactions
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/animations/FadeIn.tsx
  - src/components/sections/Hero.tsx
  - src/components/sections/About.tsx
  - src/components/sections/Projects.tsx
autonomous: true
must_haves:
  truths:
    - "Hero text elements stagger in on load"
    - "Section content fades up as user scrolls"
  artifacts:
    - path: "src/components/animations/FadeIn.tsx"
      provides: "Reusable scroll reveal wrapper"
  key_links:
    - from: "src/components/sections/Hero.tsx"
      to: "src/components/animations/FadeIn.tsx"
      via: "import and wrap"
---

<objective>
Implement reusable entrance animations and apply them to the Hero and content sections.

Purpose: Create visual depth and lead the user's eye.
Output: Animated Hero and Sections.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-motion-interactions/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create FadeIn Animation Utilities</name>
  <files>src/components/animations/FadeIn.tsx, src/components/animations/StaggerContainer.tsx</files>
  <action>
    Create `src/components/animations/FadeIn.tsx`:
    - Props: delay (number), duration (number), direction (up/down/none), viewport amount.
    - Use `motion.div` with `initial="hidden"` and `whileInView="visible"`.
    - Variants: hidden { opacity: 0, y: 20 }, visible { opacity: 1, y: 0 }.
    
    Create `src/components/animations/StaggerContainer.tsx`:
    - Wrapper that defines `staggerChildren` on the parent variant.
  </action>
  <verify>Files exist.</verify>
  <done>Reusable animation components created.</done>
</task>

<task type="auto">
  <name>Task 2: Animate Hero Section</name>
  <files>src/components/sections/Hero.tsx</files>
  <action>
    Refactor Hero.tsx to use `StaggerContainer` and `FadeIn`.
    - H1: Delay 0
    - Subtext: Delay 0.1
    - CTA Buttons: Delay 0.2
    - Background Grid: Fade in slowly (duration 1.5s).
  </action>
  <verify>Hero elements animate sequentially.</verify>
  <done>Hero animations implemented.</done>
</task>

<task type="auto">
  <name>Task 3: Animate Content Sections</name>
  <files>src/components/sections/About.tsx, src/components/sections/Projects.tsx, src/components/sections/Experience.tsx</files>
  <action>
    Wrap main content blocks in `FadeIn`.
    - About text: Fade up.
    - Experience Timeline: Stagger items (if possible) or fade entire block.
    - Projects Grid: Stagger cards (can pass `custom` prop to card index if mapped).
  </action>
  <verify>Sections reveal on scroll.</verify>
  <done>Scroll reveals active.</done>
</task>

</tasks>

<verification>
- Scroll down the page. Content should not be invisible forever (ensure viewport logic is correct, e.g., `once: true`).
- Refresh: Hero staggers correctly.
</verification>

<success_criteria>
- Reusable `FadeIn` component works.
- Hero has "premium" staggered entry.
- All sections have scroll reveals.
</success_criteria>

<output>
After completion, create `.planning/phases/03-motion-interactions/03-02-SUMMARY.md`
</output>
