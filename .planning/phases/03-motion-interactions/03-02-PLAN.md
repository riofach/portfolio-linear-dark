---
phase: 03-motion-interactions
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/sections/Hero.tsx
  - src/components/sections/About.tsx
  - src/components/sections/Contact.tsx
autonomous: true
must_haves:
  truths:
    - "Hero text animates in sequentially"
    - "About section fades up on scroll"
    - "Contact section fades up on scroll"
  artifacts:
    - path: "src/components/sections/Hero.tsx"
      contains: "motion"
  key_links:
    - from: "src/components/sections/Hero.tsx"
      to: "src/lib/motion.ts"
      via: "import SPRING_SUBTLE"
---

<objective>
Apply entrance and scroll reveal animations to Hero, About, and Contact sections.

Purpose: Create immediate visual impact on load and engagement during scroll.
Output: Animated Hero and About sections using the established motion system.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-motion-interactions/03-motion-interactions-CONTEXT.md
@.planning/phases/03-motion-interactions/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Hero Entrance Animation</name>
  <files>src/components/sections/Hero.tsx</files>
  <action>
    - Import `motion` from "motion/react".
    - Import `SPRING_SUBTLE`, `STAGGER` from `@/lib/motion`.
    - Convert container to `motion.div` with `STAGGER` variant.
    - Convert Headline, Subtext, and CTA buttons to `motion.div` (or motion.h1/p).
    - Apply `FADE_UP` variants to children.
    - Ensure background grid appears instantly (no animation) per decisions.
    - Add slight initial delay (0.2s) to container.
  </action>
  <verify>grep "motion.h1" src/components/sections/Hero.tsx</verify>
  <done>Hero elements stagger in on page load.</done>
</task>

<task type="auto">
  <name>Task 2: Section Scroll Reveals</name>
  <files>src/components/sections/About.tsx, src/components/sections/Contact.tsx</files>
  <action>
    - Import `FadeIn` from `@/components/ui/motion-wrapper`.
    - Wrap the main content of `About.tsx` in `<FadeIn>`.
    - Wrap the main content of `Contact.tsx` in `<FadeIn>`.
    - Ensure layout/spacing is preserved (FadeIn is a div, pass className if needed or wrap inner content).
  </action>
  <verify>grep "FadeIn" src/components/sections/About.tsx</verify>
  <done>Sections fade up when scrolling into view.</done>
</task>

</tasks>

<verification>
- Hero.tsx uses motion primitives and stagger
- About.tsx uses FadeIn wrapper
- Layout structure remains identical (classes preserved)
</verification>

<success_criteria>
- Hero text staggers in
- About/Contact sections reveal on scroll
- No layout shifts caused by animation wrappers
</success_criteria>

<output>
After completion, create .planning/phases/03-motion-interactions/03-02-SUMMARY.md
</output>
