---
phase: 03-motion-interactions
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ui/ProjectCard.tsx
  - src/components/ui/SkillCard.tsx
  - src/components/ui/Button.tsx
  - src/data/skills.ts
  - src/types/index.ts
autonomous: true
must_haves:
  truths:
    - "Buttons have hover and active states"
    - "Project cards scale or glow on hover"
    - "Skill cards interact with mouse movement"
  artifacts:
    - path: "src/components/ui/ProjectCard.tsx"
      provides: "Interactive card component"
  key_links:
    - from: "src/components/sections/Projects.tsx"
      to: "src/components/ui/ProjectCard.tsx"
      via: "rendering"
---

<objective>
Implement micro-interactions for interactive elements (cards, buttons) to enhance tactile feel.

Purpose: Provide feedback and delight.
Output: Interactive components.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Button Component</name>
  <files>src/components/ui/Button.tsx</files>
  <action>
    Enhance `Button.tsx`:
    - Add `whileHover={{ scale: 1.05 }}` (using motion.button).
    - Add `whileTap={{ scale: 0.95 }}`.
    - Ensure focus states are visible (ring-2).
  </action>
  <verify>Buttons respond to hover/click.</verify>
  <done>Button interactions added.</done>
</task>

<task type="auto">
  <name>Task 2: Enhance Project Cards</name>
  <files>src/components/ui/ProjectCard.tsx</files>
  <action>
    Update `ProjectCard.tsx`:
    - Convert to `motion.div`.
    - Add hover state: Slight scale up (1.02), border color change, or shadow glow.
    - Ensure entire card is clickable or links are clear.
  </action>
  <verify>Project cards feel interactive.</verify>
  <done>Project Card hover effects active.</done>
</task>

<task type="auto">
  <name>Task 3: Enhance Skill Cards & Fix Types</name>
  <files>src/components/ui/SkillCard.tsx, src/types/index.ts, src/data/skills.ts</files>
  <action>
    1. Fix Type Issues:
       - Check `src/types/index.ts` for `Skill` interface. Add `hoverColor` (string) and `svg` (ReactNode or string) if needed, or remove references in `Skills.tsx` if they shouldn't exist.
       - *Decision:* Clean up `Skills.tsx` to match the data structure. If `hoverColor` is desired, add it to `data/skills.ts`.
    2. Add Hover Effect:
       - `SkillCard` should glow with its specific `hoverColor` (or default primary) on hover.
       - Use `framer-motion` for smooth color transition.
  </action>
  <verify>LSP errors resolved. Cards glow on hover.</verify>
  <done>Skill cards fixed and interactive.</done>
</task>

</tasks>

<verification>
- Hover over all buttons.
- Hover over project cards.
- Hover over skill cards.
- Check console/LSP for type errors.
</verification>

<success_criteria>
- No type errors in Skills section.
- Consistent micro-interactions across UI.
</success_criteria>

<output>
After completion, create `.planning/phases/03-motion-interactions/03-03-SUMMARY.md`
</output>
