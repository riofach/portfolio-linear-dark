---
phase: 03-motion-interactions
plan: 03
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/sections/Skills.tsx
  - src/components/sections/Projects.tsx
autonomous: true
must_haves:
  truths:
    - "Skill cards glow on hover"
    - "Project cards scale/glow on hover"
    - "Grids stagger their content"
  artifacts:
    - path: "src/components/sections/Skills.tsx"
      contains: "whileHover"
  key_links:
    - from: "src/components/sections/Projects.tsx"
      to: "src/lib/motion.ts"
      via: "import SPRING_SUBTLE"
---

<objective>
Implement micro-interactions for interactive cards in Skills and Projects sections.

Purpose: Provide tactile feedback to user actions, enhancing the "premium" feel.
Output: Interactive Skill and Project cards with hover states and staggered entry.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-motion-interactions/03-motion-interactions-CONTEXT.md
@.planning/phases/03-motion-interactions/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Skills Grid Interaction</name>
  <files>src/components/sections/Skills.tsx</files>
  <action>
    - Wrap grid container in `<StaggerContainer>` (or use `motion.div` with STAGGER).
    - Convert individual Skill cards to `motion.div`.
    - Add `whileHover` prop:
      - Border glow effect (using an absolute overlay or border color change).
      - Subtle scale (1.02) if appropriate, or just brightness/glow.
    - Apply `FADE_UP` variant for entry animation.
  </action>
  <verify>grep "whileHover" src/components/sections/Skills.tsx</verify>
  <done>Skill cards stagger in and respond to hover.</done>
</task>

<task type="auto">
  <name>Task 2: Projects Card Interaction</name>
  <files>src/components/sections/Projects.tsx</files>
  <action>
    - Wrap projects grid in `<StaggerContainer>`.
    - Convert Project Card to `motion.div`.
    - Add `whileHover` prop:
      - Scale: 1.02
      - Border highlight: increase opacity of border
      - Image zoom: subtle scale of inner image (if feasible without major refactor).
    - Ensure `transition` uses `SPRING_SUBTLE`.
  </action>
  <verify>grep "scale" src/components/sections/Projects.tsx</verify>
  <done>Project cards have "Linear-like" hover physics.</done>
</task>

</tasks>

<verification>
- Skills.tsx has hover effects and staggering
- Projects.tsx has hover effects and staggering
- No layout breakage on hover (overflow handling)
</verification>

<success_criteria>
- Users perceive a response when hovering cards
- Content loads with a pleasing cascade effect
</success_criteria>

<output>
After completion, create .planning/phases/03-motion-interactions/03-03-SUMMARY.md
</output>
