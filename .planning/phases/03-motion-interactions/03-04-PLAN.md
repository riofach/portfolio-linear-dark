---
phase: 03-motion-interactions
plan: 04
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/components/sections/Contact.tsx
  - package.json
autonomous: true
must_haves:
  truths:
    - "Contact form validates input"
    - "Submit button shows loading state"
    - "Success message appears after simulation"
  artifacts:
    - path: "src/components/sections/Contact.tsx"
      contains: "zodResolver"
  key_links:
    - from: "src/components/sections/Contact.tsx"
      to: "zod"
      via: "import"
---

<objective>
Add interactivity and validation to the Contact Form.

Purpose: Transform the static form into a functional (simulated) interface with proper feedback states.
Output: Validated form using Zod + React Hook Form.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-motion-interactions/03-motion-interactions-CONTEXT.md
@.planning/phases/03-motion-interactions/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Validation Libraries</name>
  <files>package.json</files>
  <action>
    Install `zod` and `react-hook-form` and `@hookform/resolvers`.
  </action>
  <verify>npm list zod react-hook-form</verify>
  <done>Libraries installed.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Form Logic</name>
  <files>src/components/sections/Contact.tsx</files>
  <action>
    - Define Zod schema (name, email, message).
    - Integrate `useForm` hook.
    - Connect inputs to `register`.
    - Add error message UI (red text below inputs).
    - Create `onSubmit` handler:
      - Set `isSubmitting` state.
      - Simulate delay (1.5s).
      - Set `isSuccess` state.
      - Show success toast or inline message.
      - Reset form.
    - Animate the button state (Loading spinner / Success check).
  </action>
  <verify>grep "useForm" src/components/sections/Contact.tsx</verify>
  <done>Form validates and simulates submission cycle.</done>
</task>

</tasks>

<verification>
- Contact.tsx imports hook-form
- Error states are handled visually
</verification>

<success_criteria>
- User cannot submit empty form
- User sees feedback during and after submission
</success_criteria>

<output>
After completion, create .planning/phases/03-motion-interactions/03-04-SUMMARY.md
</output>
