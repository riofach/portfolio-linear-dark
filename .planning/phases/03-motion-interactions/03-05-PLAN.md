---
phase: 03-motion-interactions
plan: 05
type: execute
wave: 3
depends_on: [03-02, 03-03, 03-04]
files_modified:
  - src/components/sections/Experience.tsx
  - src/app/layout.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
autonomous: false
must_haves:
  truths:
    - "Timeline items appear sequentially"
    - "Lighthouse score is acceptable (>90)"
    - "SEO metadata is present"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "sitemap.xml"
  key_links:
    - from: "src/components/sections/Experience.tsx"
      to: "src/lib/motion.ts"
      via: "import STAGGER"
---

<objective>
Apply final animations to Experience section and perform final polish/SEO setup.

Purpose: Complete the motion pass and ensure the site is production-ready.
Output: Animated timeline, SEO assets, and verified performance.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-motion-interactions/03-motion-interactions-CONTEXT.md
@.planning/phases/03-motion-interactions/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Experience Timeline Animation</name>
  <files>src/components/sections/Experience.tsx</files>
  <action>
    - Wrap timeline list in `<StaggerContainer>` or `motion.ul`.
    - Convert timeline items to `motion.li`.
    - Apply `FADE_UP` or specific `FADE_IN_LEFT` variants.
    - Ensure the vertical connecting line grows or fades in (if possible via simple CSS transition or motion).
  </action>
  <verify>grep "motion.li" src/components/sections/Experience.tsx</verify>
  <done>Experience items cascade in.</done>
</task>

<task type="auto">
  <name>Task 2: SEO & Metadata</name>
  <files>src/app/layout.tsx, src/app/sitemap.ts, src/app/robots.ts</files>
  <action>
    - Update `metadata` export in `layout.tsx`:
      - Title: "Name - Portfolio"
      - Description: "Frontend Developer specializing in..."
      - OpenGraph: basic config.
    - Create `src/app/sitemap.ts` (Next.js 14+ format).
    - Create `src/app/robots.ts`.
  </action>
  <verify>test -f src/app/sitemap.ts</verify>
  <done>Basic SEO plumbing exists.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Full animation suite and interactions</what-built>
  <how-to-verify>
    1. Run `npm run dev`
    2. Open http://localhost:3000
    3. Verify Hero stagger on load.
    4. Scroll down - check About/Contact reveal.
    5. Check Skills/Project hover effects.
    6. Check Experience timeline cascade.
    7. Test Contact form validation (try empty submit).
    8. Check native scroll feel (no lag).
  </how-to-verify>
  <resume-signal>Type "approved" if animations look smooth and polished.</resume-signal>
</task>

</tasks>

<verification>
- Experience.tsx uses motion
- Metadata is populated
- Lighthouse scores (run manually) are high
</verification>

<success_criteria>
- Site feels alive and polished
- No accessibility violations from animations (reduced motion respected automatically by Motion)
</success_criteria>

<output>
After completion, create .planning/phases/03-motion-interactions/03-05-SUMMARY.md
</output>
