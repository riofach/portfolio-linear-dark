---
phase: 03-motion-interactions
plan: 05
type: execute
wave: 3
depends_on: [03-01, 03-02, 03-03, 03-04]
files_modified:
  - src/app/layout.tsx
  - src/app/sitemap.ts
  - src/app/robots.ts
  - src/app/manifest.ts
autonomous: true
must_haves:
  truths:
    - "Site has correct title and description in tab"
    - "Link previews (OpenGraph) work"
    - "Search engines can crawl the site"
  artifacts:
    - path: "src/app/sitemap.ts"
      provides: "Sitemap XML"
  key_links:
    - from: "src/app/layout.tsx"
      to: "metadata API"
      via: "export metadata"
---

<objective>
Finalize SEO metadata, manifest, and perform a self-check of performance.

Purpose: Ensure the site is production-ready.
Output: SEO-optimized codebase.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update Metadata</name>
  <files>src/app/layout.tsx</files>
  <action>
    Update `metadata` object:
    - Title template: "%s | [Name] - [Role]"
    - Description: "Portfolio of [Name], [Role] specializing in..."
    - OpenGraph: title, description, url, siteName, locale, type.
    - Twitter: card, title, description.
  </action>
  <verify>Check elements in head.</verify>
  <done>Metadata complete.</done>
</task>

<task type="auto">
  <name>Task 2: Create Sitemap and Robots</name>
  <files>src/app/sitemap.ts, src/app/robots.ts</files>
  <action>
    1. `src/app/sitemap.ts`: Return generic sitemap array (home URL).
    2. `src/app/robots.ts`: Allow all user agents.
  </action>
  <verify>Files exist.</verify>
  <done>SEO files created.</done>
</task>

<task type="auto">
  <name>Task 3: Create Manifest</name>
  <files>src/app/manifest.ts</files>
  <action>
    Create `src/app/manifest.ts`:
    - Name, short_name, description.
    - start_url: "/"
    - display: "standalone"
    - background_color, theme_color.
    - icons (generic placeholder or skip if no assets, just code the structure).
  </action>
  <verify>File exists.</verify>
  <done>Manifest created.</done>
</task>

<task type="checkpoint:human-verify" gate="optional">
  <what-built>Full site with motion and SEO.</what-built>
  <how-to-verify>
    1. Run `npm run build`.
    2. Run `npm start`.
    3. Run a Lighthouse audit in Chrome DevTools.
    4. Check for layout shifts (CLS) caused by animations.
  </how-to-verify>
  <resume-signal>Type "verified" if score is good.</resume-signal>
</task>

</tasks>

<verification>
- View page source to see meta tags.
- Verify /sitemap.xml and /robots.txt exist (build/start needed or dev).
</verification>

<success_criteria>
- Metadata populated.
- Standard SEO files present.
</success_criteria>

<output>
After completion, create `.planning/phases/03-motion-interactions/03-05-SUMMARY.md`
</output>
