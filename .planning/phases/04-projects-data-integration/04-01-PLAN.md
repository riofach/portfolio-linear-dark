---
phase: 04-projects-data-integration
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/data/projects.ts
  - src/components/ui/ProjectCard.tsx
  - src/components/sections/Projects.tsx
autonomous: true
user_setup: []
must_haves:
  truths:
    - "Projects section renders content dynamically from data file"
    - "Project cards retain exact 'linear-dark' styling (glow, gradient, border)"
    - "Hover interactions (scale 1.05, shadow) function identically to hardcoded version"
  artifacts:
    - path: "src/data/projects.ts"
      provides: "Project content source of truth"
      contains: "export const projects"
    - path: "src/components/ui/ProjectCard.tsx"
      provides: "Reusable card component with styling"
      contains: "bg-card-dark"
  key_links:
    - from: "src/components/sections/Projects.tsx"
      to: "src/data/projects.ts"
      via: "import { projects }"
      pattern: "projects\\.map"
---

<objective>
Refactor the Projects section to satisfy Requirement SEC-04 by moving hardcoded content into the data layer and unifying styling in the reusable ProjectCard component.

Purpose: Decouple content from presentation to enable easier updates and future CMS integration, while fixing the specific gap identified in the v1 audit.
Output: A fully data-driven Projects section that looks visually identical to the previous hardcoded version.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/04-projects-data-integration/04-RESEARCH.md
@src/components/sections/Projects.tsx
@src/components/ui/ProjectCard.tsx
@src/data/projects.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sync Data Layer</name>
  <files>src/data/projects.ts</files>
  <action>
    Read `src/components/sections/Projects.tsx` to extract the current hardcoded project details (Title, Description, Tags, Image paths, Links).
    Update `src/data/projects.ts` to exactly match this content.
    Ensure the `Project` type definition in the data file (or shared type file) supports all fields used in the UI (including specific image paths like `/project1.png` etc).
  </action>
  <verify>
    Cat `src/data/projects.ts` and verify it contains the real project data, not placeholders.
  </verify>
  <done>
    `src/data/projects.ts` reflects the actual portfolio content currently visible in the UI.
  </done>
</task>

<task type="auto">
  <name>Task 2: Port Styling to ProjectCard</name>
  <files>src/components/ui/ProjectCard.tsx</files>
  <action>
    Refactor `src/components/ui/ProjectCard.tsx` to match the visual implementation found in `src/components/sections/Projects.tsx`.
    
    Critical styles to transfer:
    1. Container: `bg-card-dark`, `border-white/10`, `hover:border-primary/50`, `hover:shadow-[0_0_30px_rgba(91,19,236,0.1)]`
    2. Image Wrapper: `aspect-video`, `bg-gray-900`, `group-hover:scale-105`
    3. Gradient Overlay: `bg-gradient-to-t from-card-dark to-transparent` (Crucial for text readability)
    
    Ensure the component accepts a `project` prop and correctly maps all fields (title, desc, tags, links). Use `next/image` for the project thumbnail.
  </action>
  <verify>
    Grep `src/components/ui/ProjectCard.tsx` for "bg-card-dark" and "from-card-dark".
  </verify>
  <done>
    ProjectCard component encapsulates the full "linear-dark" visual specification.
  </done>
</task>

<task type="auto">
  <name>Task 3: Wire Projects Section</name>
  <files>src/components/sections/Projects.tsx</files>
  <action>
    Refactor `src/components/sections/Projects.tsx`:
    1. Import `projects` from `@/data/projects`.
    2. Import `ProjectCard` from `@/components/ui/ProjectCard`.
    3. Replace the hardcoded JSX card blocks with a map: `{projects.map((project, index) => (...))}`.
    4. Maintain the existing `StaggerContainer` and `FADE_UP` motion variants wrapper around each card.
    5. Pass `index` if needed for staggering, though `StaggerContainer` usually handles children stagger automatically.
  </action>
  <verify>
    `npm run build` to ensure type safety.
    Visually check that no hardcoded project text remains in the file.
  </verify>
  <done>
    Projects section is cleaner, shorter, and fully data-driven.
  </done>
</task>

</tasks>

<verification>
Verify that the `Projects` section renders the exact same content and visual style as before, but the code is now DRY and data-driven.
</verification>

<success_criteria>
- `data/projects.ts` contains the definitive content.
- `Projects.tsx` loops over data.
- Hover effects (scale, glow) persist.
- No visual regression from Phase 3 state.
</success_criteria>

<output>
After completion, create `.planning/phases/04-projects-data-integration/04-01-SUMMARY.md`
</output>
