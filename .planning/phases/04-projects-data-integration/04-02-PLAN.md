---
phase: 04-projects-data-integration
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - next.config.ts
autonomous: true
must_haves:
  truths:
    - "Project images load without config error"
    - "External google user content images are valid sources"
  artifacts:
    - path: "next.config.ts"
      provides: "Image domain configuration"
      contains: "remotePatterns"
  key_links:
    - from: "src/components/ui/ProjectCard.tsx"
      to: "next.config.ts"
      via: "next/image hostname validation"
---

<objective>
Fix the Next.js image configuration error by allowing the external image hostname used in the projects data.

Purpose: Enable project images to load correctly in production and dev.
Output: Updated `next.config.ts` with `remotePatterns`.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/data/projects.ts
@next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Configure Next.js Images</name>
  <files>next.config.ts</files>
  <action>
    Update `next.config.ts` to include `images` configuration.
    Add `remotePatterns` entry for protocol `https`, hostname `lh3.googleusercontent.com`.
    Ensure strict type safety is maintained in the config file.
  </action>
  <verify>
    Run `npm run build` to verify config validity.
    (Runtime verification requires visiting the page).
  </verify>
  <done>
    `next.config.ts` contains `remotePatterns` with the correct hostname.
  </done>
</task>

</tasks>

<verification>
- `npm run dev` should no longer show the "Invalid src prop" error.
- Project cards should display images.
</verification>

<success_criteria>
- [ ] User can view project images without error overlay.
</success_criteria>

<output>
After completion, create `.planning/phases/04-projects-data-integration/04-02-SUMMARY.md`
</output>
