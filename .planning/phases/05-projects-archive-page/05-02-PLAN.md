---
phase: 05-projects-archive-page
plan: 02
type: execute
wave: 2
depends_on: ["05-01"]
files_modified:
  - src/app/allprojects/page.tsx
autonomous: true

must_haves:
  truths:
    - "Page renders at /allprojects"
    - "Projects grid displays all projects from projects.ts"
    - "Header shows correct title (All Projects) and counter"
    - "Filters are NOT displayed"
    - "Load More button exists at bottom"
  artifacts:
    - path: "src/app/allprojects/page.tsx"
      provides: "Projects Archive Page"
    - path: "src/data/projects.ts"
      provides: "Project data source"
  key_links:
    - from: "src/app/allprojects/page.tsx"
      to: "src/components/ui/ProjectCard.tsx"
      via: "import and render"
---

<objective>
Implement the Projects Archive page (`/allprojects`) reusing the existing design system and components.

Purpose: Provide a dedicated view for the full project portfolio without cluttering the homepage.
Output: A fully functional archive page matching the linear-dark aesthetic.
</objective>

<execution_context>
@./.opencode/get-shit-done/workflows/execute-plan.md
@./.opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@src/data/projects.ts
@src/components/ui/ProjectCard.tsx
@_reference/projects.html
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Archive Page Structure</name>
  <files>src/app/allprojects/page.tsx</files>
  <action>
    Create the page component at `src/app/allprojects/page.tsx`.
    
    Structure:
    1. **Background**: Use `bg-grid-pattern` (created in Plan 01) and a radial glow effect (div with blur) in the header area.
    2. **Container**: `max-w-7xl mx-auto px-6`.
    3. **Header Section**:
       - Title: "All Projects" (H1, matching reference typography).
       - Counter: Display dynamic count `projects.length`.
       - Subtitle: "A collection of projects..." (text from reference).
    4. **NO FILTERS**: Explicitly exclude the filter tabs seen in the reference.
    
    Technical:
    - Import `projects` from `@/data/projects`.
    - Use `main` as the root element.
    - Ensure spacing matches `pt-24 pb-16` (to clear fixed header).
  </action>
  <verify>ls src/app/allprojects/page.tsx</verify>
  <done>Page file created with correct basic structure</done>
</task>

<task type="auto">
  <name>Task 2: Implement Grid and Load More</name>
  <files>src/app/allprojects/page.tsx</files>
  <action>
    Render the project grid and footer button.
    
    1. **Grid**:
       - Use CSS Grid: `grid grid-cols-1 lg:grid-cols-2 gap-8`.
       - Map through `projects` array.
       - Render `ProjectCard` for each item. Pass an `index` for stagger delay if supported by the card (or wrap in `StaggerContainer` if available).
    
    2. **Load More**:
       - Add a button at the bottom: "Load More".
       - Visual only: Since we have few projects, it can be disabled or just a static button that does nothing (or shows a toast).
       - Style: Linear-dark secondary button style (border, subtle hover).
       
    Note: Do NOT implement complex pagination logic yet. Visual parity is the goal.
  </action>
  <verify>grep "ProjectCard" src/app/allprojects/page.tsx</verify>
  <done>Projects displayed in grid and Load More button visible</done>
</task>

</tasks>

<verification>
- Page loads at `/allprojects`
- Visual match with linear-dark theme (dark background, grid pattern, correct typography)
- All projects from data file are visible
</verification>

<success_criteria>
- /allprojects route is accessible
- Design matches established project aesthetic
- Global Header/Footer wrap the content (inherited from layout)
</success_criteria>

<output>
After completion, create `.planning/phases/05-projects-archive-page/05-02-SUMMARY.md`
</output>
